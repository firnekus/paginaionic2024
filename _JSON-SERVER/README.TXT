1. Para instalar json-server:

npm install -g json-server

2. Para ejecutar json-server:

json-server --watch posts.json

json-server --watch db.json --host 192.168.179.91 --port 8105

cd_JSON-SERVER

json-server --watch posts.json --host localhost -- port 3005
ionic serve --host=localhost --port=8100